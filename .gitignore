# Source material (certificates, slide decks, raw video) - only the converted
# .md files are versioned; everything else is scratch input for convert-to-md.py
courses/**/*.pdf
courses/**/*.pptx
courses/**/*.ppt
courses/**/*.txt
*.mp4

# Windows/WSL download metadata (NTFS Alternate Data Stream)
*:Zone.Identifier

# Misc
.DS_Store
__pycache__/
*.pyc
